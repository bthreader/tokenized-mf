// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.13;

contract GenericTest {
    // Helpful test accounts
    address internal acc1 = address(0x1);
    address internal acc2 = address(0x2);
    address internal acc3 = address(0x3);
}